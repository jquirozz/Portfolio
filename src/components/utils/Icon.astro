---
const { name, size } = Astro.props;
---

<!-- For static-sized icons -->
<img
  class="Icon"
  src={`/icons/${name}.svg`}
  alt={`${name} icon`}
  style={`width: ${size}rem;`}
/>

<style>
  .Icon {
    aspect-ratio: 1;
    object-fit: contain;
  }
</style>
